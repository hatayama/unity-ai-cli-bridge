# Unity AI CLI Bridge Companion

This package adds Unity-side diagnostics and helper menus for `unity-ai-cli`.

It does not modify `com.unity.ai.assistant`. It only reads public bridge state and writes companion diagnostics snapshots under `Library/UnityAiCliBridge/`.
